# Streamlit for web interface
streamlit>=1.55.0,<1.56.0

# NeMo Guardrails for AI safety
nemoguardrails>=0.19.0,<0.20
 
# NVIDIA NAT (NeMo Agent Toolkit) with extensions
nvidia-nat[langchain]~=1.3
nvidia-nat[telemetry]~=1.3

# Vector search
usearch~=2.21.0

# OpenTelemetry for observability
opentelemetry-sdk>=1.40.0,<1.41.0
opentelemetry-exporter-otlp>=1.40.0,<1.41.0
traceloop-sdk==0.51.1
opentelemetry-instrumentation-langchain==0.52.6
